/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [{
            protocol: 'https',
            hostname: 'xgbfuvduzaljhyepdmyk.supabase.co',
            port: ''
        }]
    }
};

export default nextConfig;